# Copy thành .env và điền giá trị thật. KHÔNG commit .env.
# Lấy ở Supabase Dashboard → Project Settings → API.
VITE_SUPABASE_URL=https://tnskhshfvmimihlnzbly.supabase.co
VITE_SUPABASE_ANON_KEY=paste-anon-key-o-day
# Nơi phục vụ model ONNX (mặc định cùng host Cloudflare Pages / dist)
VITE_MODEL_BASE_URL=/PaddleOCR-Models

# service_role CHỈ dùng cho migration/seed phía server (SQL Editor),
# TUYỆT ĐỐI không đưa vào VITE_* (lộ ra browser = mất toàn bộ RLS).
